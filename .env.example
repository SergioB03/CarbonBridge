# Copy to .env (or set in Amplify → Environment variables).
# URL of the Bedrock copilot Lambda Function URL. Leave unset to run the UI
# without a backend (the Copilot panel shows a "not wired" state).
VITE_COPILOT_URL=
